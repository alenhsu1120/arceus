# Claude Code 安裝與使用指南

# 1. 系統需求

## 支援環境

Claude Code 支援：

- Linux
- macOS
- Windows PowerShell


## 建議開發環境

推薦架構：

```text
Windows PC
 |
 ├── VSCode
 |
 └── Remote SSH
          |
          v
Linux Server / VM
 |
 ├── Git Repository
 ├── Node.js
 └── Claude Code
```

---

# 2. 安裝 Node.js

Claude Code 需要 Node.js 環境。

確認 Node.js：

```bash
node -v
```

確認 npm：

```bash
npm -v
```

建議版本：

```text
Node.js >= 18
```

---

## 2.1 Rocky Linux 安裝 Node.js

查看是否已安裝：

```bash
node --version
```

如果沒有安裝：

```bash
sudo dnf install nodejs npm
```

確認：

```bash
node -v
npm -v
```

---

## 2.2 Ubuntu / Debian 安裝 Node.js

更新套件：

```bash
sudo apt update
```

安裝：

```bash
sudo apt install nodejs npm
```

確認：

```bash
node -v
npm -v
```

---

# 3. 安裝 Claude Code

## 5.1 Linux / macOS 安裝

執行：

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

---

## 3.2 Windows PowerShell 安裝

開啟 PowerShell：

```powershell
irm https://claude.ai/install.ps1 | iex
```

---

# 4. 確認 Claude Code 安裝成功

查看版本：

```bash
claude --version
```

成功會顯示：

```text
Claude Code version x.x.x
```

---

# 5. 登入 Claude 帳號

第一次使用：

```bash
claude
```

系統會要求登入。

操作流程：

1. Terminal 顯示登入網址
2. 複製網址到瀏覽器
3. 登入 Claude 帳號
4. 完成授權


成功後：

```text
Logged in successfully
```

---

# 6. 在 Git 專案中使用 Claude Code

進入專案目錄：

```bash
cd your-project
```

例如：

```bash
cd ~/wisdomfgw
```

啟動 Claude：

```bash
claude
```

啟動後即可輸入需求。

---


# 7. VSCode 搭配 Claude Code

推薦使用方式：

```text
VSCode
 |
 | Remote SSH
 |
 v
Linux VM / Server
 |
 Git Repository
 |
 Claude Code
```

操作流程：

1. 安裝 VSCode Remote SSH Extension
2. SSH 登入 Linux Server
3. 開啟 Git 專案
4. 開啟 Terminal


執行：

```bash
claude
```

即可讓 Claude Code 分析目前專案。

---
